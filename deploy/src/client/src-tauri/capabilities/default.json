{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-capability",
  "description": "Capability for the local server selector window.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
