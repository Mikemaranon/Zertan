{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "workspace-capability",
  "description": "Capability for the remote workspace window theme bridge.",
  "windows": ["workspace"],
  "permissions": ["core:default"]
}
